# Atlassian MCP Server environment configuration.
# Copy this file to .env and fill in the values before running the server.

# Optional shared Atlassian site fallback (used when service-specific sites are omitted).
ATLASSIAN_SITE=

# Jira deployment URL (UI base). Provide scheme explicitly.
ATLASSIAN_JIRA_SITE=https://jira.example.com
# Optional override for Jira REST API base.
ATLASSIAN_JIRA_API_BASE=

# Jira credentials: either supply email/api token or an OAuth token (leave unused fields blank).
ATLASSIAN_JIRA_EMAIL=jira-bot@example.com
ATLASSIAN_JIRA_API_TOKEN=your-jira-api-token
ATLASSIAN_JIRA_OAUTH_TOKEN=

# Confluence deployment URL (UI base). Provide scheme explicitly.
ATLASSIAN_CONFLUENCE_SITE=https://confluence.example.com
# Optional override for Confluence REST API base.
ATLASSIAN_CONFLUENCE_API_BASE=

# Confluence credentials: either supply email/api token or an OAuth token (leave unused fields blank).
ATLASSIAN_CONFLUENCE_EMAIL=confluence-bot@example.com
ATLASSIAN_CONFLUENCE_API_TOKEN=your-confluence-api-token
ATLASSIAN_CONFLUENCE_OAUTH_TOKEN=

# Optional: override the log level (debug, info, warn, error).
SERVER_LOG_LEVEL=info

# Optional: enable integration tests when credentials are configured.
MCP_INTEGRATION=
