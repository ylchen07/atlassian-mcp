# Go cache directories (local to project)
export GOCACHE="$(pwd)/.cache/go-build"
export GOMODCACHE="$(pwd)/.cache/go-mod"

# Optional: Load environment variables from .env if it exists
if [ -f .env ]; then
  dotenv .env
fi
