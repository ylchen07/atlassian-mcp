# Go caches (build cache and module cache)
.cache/

# Executables
bin/
*.exe
*.dll
*.so

# Environment files with secrets
.env
.env.local
# Note: .envrc is committed as it contains project config, not secrets

# IDE files
.vscode/
.idea/
